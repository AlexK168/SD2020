<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NewGameActivity">

    <Button
        android:id="@+id/createBoardButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/place_ships"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/gameIdTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="@string/game_id"
        app:layout_constraintEnd_toStartOf="@+id/gameIdValueTextView"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/createBoardButton" />

    <EditText
        android:id="@+id/gameIdValueTextView"
        android:layout_width="139dp"
        android:layout_height="40dp"
        app:layout_constraintBottom_toBottomOf="@+id/gameIdTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toEndOf="@+id/gameIdTextView"
        app:layout_constraintTop_toTopOf="@+id/gameIdTextView" />

    <Button
        android:id="@+id/toBattleButton"
        android:layout_width="132dp"
        android:layout_height="59dp"
        android:text="@string/to_battle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/createBoardButton" />
</androidx.constraintlayout.widget.ConstraintLayout>