<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.awesometimer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AwesomeTimer">
        <activity android:name=".Activities.EditItem"></activity>
        <activity android:name=".Activities.Stages" />
        <activity
            android:name=".Activities.EditSequence"
            android:label="@string/title_activity_edit_sequence"
            android:theme="@style/Theme.AwesomeTimer.NoActionBar" />
        <activity android:name=".Activities.AddStage" />
        <activity android:name=".Activities.AddSequence" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>